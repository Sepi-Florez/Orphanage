using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Eulerprob02 : MonoBehaviour
{
    #region Deprecated
    /*
    public int maxNumber = 10 , b = 3;

    void Start ()
    {
        int multiple = maxNumber / b;
        print(multiple);

        for(int sum = 0; sum <= maxNumber; sum += multiple)
        {
            //print(Mathf.Pow(multiple, sum));
            print(sum * multiple);

            //int i = sum * multiple;

            //answer = i;
        }
        //for(int a = multiple)
        //print(answer);
	}*/
    #endregion
}
